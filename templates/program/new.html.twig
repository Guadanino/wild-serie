{% extends "base.html.twig" %}

{% block body %}
	<section class="body">
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit">Add category</button>
		{{ form_end(form) }}
	</section>
{% endblock %}
